name: Workflow 1

on:
  workflow_dispatch:

jobs:
  add_content:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Create file
        run: |
          echo "Hello World"

     
