name: Workflow 1

on:
  workflow_dispatch:

jobs:
 build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

     
      - name: Write to file
        run: |
          file_path="Sample.txt"
          echo "This content will overwrite the file." > "$file_path"
